apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tonaiagent.fullname" . }}
  labels:
    {{- include "tonaiagent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
